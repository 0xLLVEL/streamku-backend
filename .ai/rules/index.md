# Rules Index

| Glob | Rule files |
| --- | --- |
| `**/*` | ../CONTEXT.md, PROJECT-MAP.md |

> Read every rule file whose globs cover the path(s) in scope before planning or editing. `CONTEXT.md` (repo root) is the domain glossary; `PROJECT-MAP.md` is the implementation map. Run `grep -rin '<keyword>' .ai/rules` to catch rules a path match alone misses.