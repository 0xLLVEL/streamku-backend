meta {
  name: Admin Movies Cast Destroy
  type: http
  seq: 1
}

delete {
  url: {{base_url}}/api/v1/admin/movies/{movie}/cast/{cast}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
