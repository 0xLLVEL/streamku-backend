meta {
  name: Admin Movies Videos Destroy
  type: http
  seq: 1
}

delete {
  url: {{base_url}}/api/v1/admin/movies/{movie}/videos/{video}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
