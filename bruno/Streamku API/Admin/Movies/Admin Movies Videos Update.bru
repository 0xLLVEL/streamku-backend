meta {
  name: Admin Movies Videos Update
  type: http
  seq: 1
}

put {
  url: {{base_url}}/api/v1/admin/movies/{movie}/videos/{video}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
