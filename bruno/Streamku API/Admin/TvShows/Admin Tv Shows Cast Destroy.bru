meta {
  name: Admin Tv Shows Cast Destroy
  type: http
  seq: 1
}

delete {
  url: {{base_url}}/api/v1/admin/tv-shows/{tvShow}/cast/{cast}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
