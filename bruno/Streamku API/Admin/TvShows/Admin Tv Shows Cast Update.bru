meta {
  name: Admin Tv Shows Cast Update
  type: http
  seq: 1
}

put {
  url: {{base_url}}/api/v1/admin/tv-shows/{tvShow}/cast/{cast}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
