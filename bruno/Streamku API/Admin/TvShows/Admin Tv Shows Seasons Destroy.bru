meta {
  name: Admin Tv Shows Seasons Destroy
  type: http
  seq: 1
}

delete {
  url: {{base_url}}/api/v1/admin/tv-shows/{tvShow}/seasons/{season_number}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
