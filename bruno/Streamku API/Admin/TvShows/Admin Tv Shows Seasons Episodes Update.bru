meta {
  name: Admin Tv Shows Seasons Episodes Update
  type: http
  seq: 1
}

put {
  url: {{base_url}}/api/v1/admin/tv-shows/{tvShow}/seasons/{season}/episodes/{episode}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
