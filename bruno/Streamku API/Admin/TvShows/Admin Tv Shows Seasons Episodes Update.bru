meta {
  name: Admin Tv Shows Seasons Episodes Update
  type: http
  seq: 1
}

put {
  url: {{base_url}}/api/v1/admin/tv-shows/{tvShow}/seasons/{season_number}/episodes/{episode_number}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
