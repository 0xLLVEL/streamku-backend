meta {
  name: Admin Tv Shows Seasons Store
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/admin/tv-shows/{tvShow}/seasons
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
