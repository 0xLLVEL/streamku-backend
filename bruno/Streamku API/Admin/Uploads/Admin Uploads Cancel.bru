meta {
  name: Admin Uploads Cancel
  type: http
  seq: 1
}

delete {
  url: {{base_url}}/api/v1/admin/uploads/{upload:upload_id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
