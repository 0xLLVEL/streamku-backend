meta {
  name: Admin Uploads Complete
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/admin/uploads/{upload:upload_id}/complete
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
